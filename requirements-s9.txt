# Install PyTorch 2.8.0 with CUDA 12.8 first (tested Runpod image records in report).
torch==2.8.0
triton==3.4.0
tilelang==0.1.14
liger-kernel==0.8.2
transformers==4.57.1
accelerate==1.11.0
matplotlib==3.11.1
